---
description: Сгенерировать/показать список/проверить актуальность архитектурных диаграмм проекта (tasks.md, Фаза 9)
argument-hint: "[generate <kind> [scope]|list|status [kind]]"
---

# /rootgraph-diagram

Обёртка над `rootgraph_diagram_generate`/`rootgraph_diagram_list`/
`rootgraph_diagram_status` — диаграммы (`.mmd`+`.drawio`) коммитятся в
`docs/diagrams/` репозитория пользователя, сервер сам строит Cypher-запрос
и оба рендера за один вызов, клиент только пишет файлы и `manifest.json`.

Перед первым использованием в проекте убедись, что Rootgraph подключён
(`rootgraph_status`, `configured: true`) — если нет, скажи пользователю, что
нужен `rootgraph_init`, и не выполняй ничего из этого файла.

Аргументы: `$ARGUMENTS`. Разбери первое слово как подкоманду.

## `generate <kind> [scope]`

`kind` — `components` (пакеты/директории) или `calls` (граф вызовов); если
пользователь не указал ни один из двух — спроси, какой нужен, не выбирай
за него. `scope` — необязательный префикс пути каталога, ограничивающий
диаграмму поддеревом; без него диаграмма строится по всему проекту.

Вызови `rootgraph_diagram_generate(kind, scope)`. Покажи пользователю, куда
записаны файлы, `node_count`/`edge_count` и `aggregation_level` — если
агрегация сработала (уровень выше `"none"`), явно скажи, что часть узлов
свёрнута по каталогам из-за лимита `max_nodes`, а не молчи об этом.

## `list`

Вызови `rootgraph_diagram_list` без параметров, выведи таблицей
`kind | scope | сгенерирована когда`, и отдельно — `available_kinds`,
которых ещё нет в `docs/diagrams/`, как подсказку, что ещё можно
сгенерировать.

## `status [kind]`

Вызови `rootgraph_diagram_status(kind)` — без `kind` проверяются разом все
уже сгенерированные диаграммы. Для каждой из `results`: если `stale: true`,
предложи перегенерировать (`generate` с тем же `kind`/`scope`); если в
записи есть `error` — покажи его как есть, не пытайся угадать причину.

## Без аргументов — короткий интерактивный опрос

Если `$ARGUMENTS` пуст, сначала спроси одним сообщением, что нужно:
сгенерировать новую диаграмму, посмотреть уже готовые, или проверить их
актуальность — и дальше действуй по соответствующей секции выше.
