---
description: Собрать базовые документы проекта, разобрать незавершённую работу и техдолг, дополнить архитектурную карту агентами
argument-hint: "[--refresh] [--force] [documents|triage|components|conventions]"
---

# /rootgraph-bootstrap

Доводит подключённый проект до базового набора: документы (обзор, архитектура,
модули, модель данных, инфраструктура, руководство разработчика, незавершённая
работа), разбор находок сканирования и компоненты архитектуры. Протокол,
правила и промпты субагентов — навык `rootgraph-bootstrap`.

Сначала вызови `rootgraph_status`. Если `configured: false` — скажи, что нужен
`rootgraph_init`, и остановись.

Аргументы: `$ARGUMENTS`. Разбор: `--refresh` и `--force` передаются в
`rootgraph_bootstrap_run` как есть (`--force` только если пользователь сам его
набрал: он перезаписывает ручные правки); слово `documents`, `triage`,
`components` или `conventions` — это `phase` для `rootgraph_bootstrap_plan`,
без него `phase = all`.

## Шаги

1. `rootgraph_bootstrap_status`.
2. Если шаг `base_documents` не `done` или переданы `--refresh`/`--force` —
   `rootgraph_bootstrap_run` (с этими флагами).
3. `rootgraph_bootstrap_plan(phase)` и выполни `instruction` дословно: субагенты
   через Task, все вызовы одним сообщением. Явный запуск команды считается
   согласием на ≤ 8 субагентов; если `estimated_subagents` больше 8 — назови
   число и спроси.
4. `rootgraph_bootstrap_mark(step="architecture_review", status="done", note)`.
5. Язык всех текстов — язык проекта из поля `language_name` результата;
   идентификаторы, slug и названия документов остаются английскими.
6. Итог одной строкой: `Готово: документов N, принято задач A, отклонено B, компонентов C.`

## Чего не делать

- Не запускай `--force` по своей инициативе.
- Не закрывай находки массово и не пропускай молча.
- Не делай больше 2 раундов субагентов.
